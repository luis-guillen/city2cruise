# CruiseDispatch RL microservice — serving/runtime deps, pinned for reproducibility
# (MLOps §4.4). These are the exact versions used to train the committed artifacts.
# Install: pip install -r rl_service/requirements.txt
# Training-only deps (MLflow, TensorBoard, matplotlib) live in requirements-train.txt
# so the serving image stays lean.
# NOTE: the Docker image installs torch from the CPU wheel index (see Dockerfile),
#       filtering the torch line below; the pinned version is kept in sync via
#       the TORCH_VERSION build arg.

# Web framework
fastapi==0.139.0
uvicorn[standard]==0.50.2

# Data validation
pydantic==2.13.4

# RL
gymnasium==1.3.0
stable-baselines3==2.9.0
torch==2.13.0
cloudpickle==3.1.2          # SB3 checkpoint (de)serialisation — pin for repro

# Numerics
numpy==2.5.1

# HTTP client (pulling state tensors from backend in training mode)
httpx==0.28.1

# Model observability & drift detection (MLOps §4.4)
prometheus-client==0.25.0
scipy==1.18.0
