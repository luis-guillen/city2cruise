# Sprint 3.E — CruiseDispatch RL microservice
# Install: pip install -r rl_service/requirements.txt

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29

# Data validation
pydantic>=2.7

# RL
# gymnasium replaces deprecated openai/gym (required by SB3 >=2.0)
gymnasium>=0.29
stable-baselines3>=2.3
torch>=2.2          # CPU build is sufficient; use +cu121 for GPU training

# Numerics
numpy>=1.26

# HTTP client (for pulling state tensors from backend in training mode)
httpx>=0.27

# Async task queue (optional — uvicorn handles background tasks natively)
# anyio>=4.3
