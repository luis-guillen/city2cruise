name: E2E (Playwright)

on:
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      base_url:
        description: 'BASE_URL del frontend (vacío = build local)'
        required: false
        default: ''

permissions:
  contents: read

jobs:
  e2e:
    name: Playwright (chromium + firefox + webkit)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ./cruise-connect-main
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: cruise-connect-main/pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps
      - name: Build app
        run: pnpm run build
      - name: Run E2E
        env:
          BASE_URL: ${{ github.event.inputs.base_url }}
          BACKEND_URL: ${{ secrets.STAGING_BACKEND_URL }}
        run: pnpm run e2e
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: cruise-connect-main/e2e/.report
          retention-days: 14
      - name: Upload junit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-junit
          path: cruise-connect-main/e2e/.results/junit.xml
          if-no-files-found: ignore
